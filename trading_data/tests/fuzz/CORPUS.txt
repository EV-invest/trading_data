# Previously-failing fuzz cases — one `target seed size version` per line (`#` starts a comment,
# blanks ignored). `fuzz` appends a minimal repro here automatically when it finds a NEW failure;
# fix the bug, commit the new line, and `regressions` replays it forever. Do not hand-edit the
# numbers — the trailing comment is the only free-form part.
#
# `version` is the generator fingerprint from `corpus.rs`, taken per target over the sources that
# decide what its `(seed, size)` means. A pair names a *trace* only under that generator: reshape it
# and the same pair replays some unrelated trace — a random run wearing a regression test's name,
# which is worse than no test, because it reports green for a case it never runs. So `regressions`
# replays only lines at the current version, and prints how many it skipped.
#
# NEVER DELETE A LINE. A superseded line stops being a test, but it does not stop being the record
# of what once broke and why, and that costs nothing to keep.
# The first line the fuzzer ever recorded, and not a product finding: the generator was drawing
# zero-trade messages, which `BatchTrades::new` rightly refuses. Kept because lines are never
# deleted; stale at the current fingerprint, so it is documentation and not a test.
weave 0 9 1841168572  # PANIC: panicked at trading_data_core/src/batch.rs:31:9: BatchTrades is never empty by construction
# Also not a product finding — an oracle error, and an instructive one. A step reports `arrival` as
# the *last* key of the run it emitted, so under a coarse clock two steps can share an `arrival`
# without the weaver having faced a tie at all: an anchors run whose head sits a checkpoint period
# back swallows the checkpoint sharing this step's stamp, and is emitted ahead of it on its head.
# The §1.5 tie-break is only observable under `ReadClock::EVENT`, and that is where it is now checked.
weave 256541034804199029 41 1762018250  # live: at arrival 121000000003 lane deltas follows lane anchors — the tie-break must order ["trades", "deltas", "anchors", "oi", "mc"]
# A PRODUCT FINDING, reported and not fixed (a fuzzer that finds a bug in logic it does not own says
# so and keeps going). The shape reports a `Latest` dark once the only consumer reading it through
# `Sampling` is gated shut, while the sweep advances it exactly like the `Buffer` beside it — the
# macro pins a retention cell it finds in `st.bufs` and a level is not in `st.bufs`. `Under::live`
# calls itself "a reading of the emitted code rather than a second model of it", and here it is the
# second model. The sweep is the correct side; see `LEVEL_DIVERGES` in gates.rs for the quarantine,
# which fails the moment the pin lands so nobody has to remember to remove it.
shape_sweep 0 4 1742470698  # under Hot=false Warm=false, the shape says `Latest<fuzz::fixture::Src>` is dark and the sweep advanced it
# Two more oracle errors of mine, not findings. An all-`Want::Nothing` tick observes nothing, so a
# short trace compared an unobserved run against an unobserved run; and a batch out flattens to its
# *last* element, so a run of three whose last one declines is `fires=3` with no slots — a reading of
# the last element rather than a fourth state of the out.
noninvasive 15490212636682683044 8 1429616719  # the observer was never called, so the comparison is vacuous
absence 0 4 1429616719  # tick 0, `fuzz::fixture::Total`: fires=3 says fired, the flattening says unfired — that is a third state
