[package]
name = "trading_data_persistence"
description = "Storage tier: parquet catalog, lanes, live book, replay"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
arrayvec.workspace = true
arrow = { workspace = true }
jiff.workspace = true
parquet = { workspace = true, features = ["arrow", "snap", "zstd"] }
thiserror.workspace = true
tracing.workspace = true
trading_data_core.workspace = true
trading_data_dag.workspace = true
# `Lane` writes this crate's disk encoding and the dag is storage-free, so it is named here rather
# than through the dag's macro re-export.
trading_data_macros.workspace = true
v_utils.workspace = true

[dev-dependencies]
iai-callgrind.workspace = true
tempfile = { workspace = true }

[[bench]]
name = "write_throughput"
harness = false

# One integration binary, per matklad's "delete cargo integration tests".
[[test]]
name = "integration"
path = "tests/integration/main.rs"

[lints]
workspace = true
